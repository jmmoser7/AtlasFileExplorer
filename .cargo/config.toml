# Let Rust discover the installed MSVC toolchain and Windows SDK.
[alias]
xtask = "run -p xtask --"
# Short run aliases for the edit loop (see docs/dev-loop.md).
slate = "run -p slate --"
atlas = "run -p native-file-atlas --"
