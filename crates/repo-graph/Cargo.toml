[package]
name = "repo-graph"
version.workspace = true
edition.workspace = true
description = "UI-free repository history extraction and deterministic layout for Repository Lens portals"

[dependencies]
gix = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

